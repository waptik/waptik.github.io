---
import Logo from "./Logo.astro";
import Nav from "./Nav.astro";

const { current = "" } = Astro.props;
---

<header class="flex mx-auto max-w-[1400px] p-8 w-full max-sm:px-0">
  <Logo />
  <Nav current={current} />
</header>
